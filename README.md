## Installation Command

```bash
curl -fsSL https://raw.githubusercontent.com/konair/skill-manager/main/install.sh | bash
```

Or if `sudo` is needed for `/usr/local/bin`:

```bash
curl -fsSL https://raw.githubusercontent.com/konair/skill-manager/main/install.sh | sudo bash
```
